{
  "name": "restack",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npx concurrently -n host,ui -c blue,green \"npm run dev --prefix host\" \"npm run dev --prefix ui\"",
    "build": "npm run build --prefix host && npm run build --prefix ui",
    "build:host": "npm run build --prefix host",
    "build:ui": "npm run build --prefix ui",
    "typecheck": "npm run typecheck --prefix host && npm run typecheck --prefix ui",
    "generate-types": "./scripts/generate-types.sh",
    "start": "node host/dist/index.js ui --static-root ui/dist --port 6969",
    "install:all": "npm install --prefix host && npm install --prefix ui"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
